spring:
  datasource:
    url: jdbc:mysql://localhost:3306/photo_studio
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 50MB

server:
  port: 8080

app:
  upload-dir: uploads